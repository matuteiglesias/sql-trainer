# Final Handoff

PR04 through PR06 implementation is present on the current branch. Before
release, restore npm registry access, run `npm ci`, execute every command in the
runbook, complete the production-preview smoke script, and deploy `dist/` to the
chosen static host. Only after recording that evidence should `STATUS.yaml` be
changed from blocked to closed.
